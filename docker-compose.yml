version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    networks:
      - memory-game-network

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    networks:
      - memory-game-network

networks:
  memory-game-network:
    driver: bridge